# Скопируйте в .env и заполните:  cp .env.example .env

# --- Обязательно для работы в Telegram ---

# Токен бота от @BotFather
TELEGRAM_TOKEN=

# Публичный HTTPS-адрес мини-аппа. Telegram открывает мини-аппы ТОЛЬКО по https.
# Для локальной отладки поднимите туннель: npx localtunnel --port 3000
WEBAPP_URL=https://example.com

# Telegram ID администраторов через запятую. Только они видят вкладку
# «Админ» и могут менять чужой баланс. Свой ID можно узнать у @userinfobot.
# В DEV_MODE тестовый игрок имеет ID 999000001.
ADMIN_TG_IDS=

# --- Необязательно ---

PORT=3000

# Файл базы данных SQLite
DB_PATH=./data/app.db

# Отключает проверку подписи Telegram и подставляет тестового игрока.
# Нужно, чтобы открыть игру в обычном браузере на localhost.
# В продакшене ОБЯЗАТЕЛЬНО должно быть выключено (удалите строку или false).
DEV_MODE=false

# Платежи Beeline (секреты Android-устройств регистрируются через admin API)
BEELINE_PHONE=+79990000000
PAYMENT_TTL_MINUTES=10
PAYMENT_MARKUP_MIN=1
PAYMENT_MARKUP_MAX=99
PAYMENT_MIN=500
PAYMENT_MAX=100000
BEELINE_COMPLETED_LIMIT=0
# 32 случайных байта в base64 или 64 hex-символа. Не менять после появления заявок по карте.
PAYOUT_DATA_KEY=

# Стартовый баланс нового игрока, условных единиц
STARTING_BALANCE=1000

# Бонус: сколько начисляем, как часто и при каком балансе он доступен
BONUS_AMOUNT=500
BONUS_COOLDOWN_MIN=60
BONUS_BALANCE_LIMIT=250

# Сколько секунд считается действительным initData от Telegram
INITDATA_MAX_AGE=86400
